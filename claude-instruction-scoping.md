# Claude Code 指示の領域別スコープ設計

## 背景と問題

Claude Code を開発・レビュー以外の領域（採用活動、データ解析）でも使っている。
グローバル `~/.claude/CLAUDE.md` は**作業内容に関わらず無条件に読み込まれる**ため、
そこに書かれた条項はすべて「普遍的に正しい指示」として提示される。実際には領域固有の
条項が混ざっているので、2 つの失敗が起きる。

1. **無印の領域固有ルールの誤適用** — 「長くなった関数はサブルーチン化」は採用活動には
   無関係だが、それを無関係と判断するのはモデル側の推測に委ねられる。抽象度が中間の条項
   （例: 「実装は Top-Down で提示」）を一般化されると、領域固有の作法と衝突し得る
2. **但し書きによる希薄化** — 各条項に「開発時は…、それ以外は…」を付けると、密度が上がって
   確実に適用されなくなる

同じ条項が領域によって逆向きに働く例:

- 「過不足のなさを第一にし、理解できるなら削る」 → 候補者評価に適用すると、判断根拠となった
  観察を削りかねない。採用では後から辿れることが公平性の要件なので、削る方向が誤りになり得る
- 「未検証の推測を検証済みの事実と同じ書きぶりで並べない」 → 採用ではコードレビューより
  **さらに強く**効かせたい

領域ごとに強調の置き方が変わるので、1 本の普遍条項では表現しきれない。

## 現在の構成（実測）

| パス | 状態 |
|---|---|
| `~/.claude/CLAUDE.md` | `~/dotfiles/.claude/CLAUDE.md` への symlink |
| `~/.claude/hooks` | symlink |
| `~/.claude/settings.json` | symlink |
| `~/.claude/skills` | symlink |
| `~/.claude/agents` | **存在しない** |

「`~/dotfiles/.claude/` に実体を置き、`~/.claude/` から個別に symlink」というパターンが
4 件で確立済み。`agents` は未着手。

既存スキルは `~/dotfiles/.claude/skills/` に prepare-pr / review-pr / review-session /
self-review-pr の 4 つ。すべて PR 周辺で、**実装そのものを扱うスキルは無い**。

## 最初に解決すべき未確認事項

**サブ Agent の文脈に、グローバル `~/.claude/CLAUDE.md` が引き継がれるか。**

この答えで設計が反転するので、他の作業より先に確認する。

- **引き継がれる**: 普遍条項をグローバルに 1 本置いたまま、各 Agent は領域固有の層だけを
  足せばよい。重複が起きない → Agent 構成（選択肢 D）を進める
- **引き継がれない**: 普遍条項を全 Agent に複製することになり、**現状より悪化する**
  → 選択肢 A（CLAUDE.md 内の節分け）に留める

`claude-code-guide` サブエージェントまたは公式ドキュメントで確認できる。

## 条項の分類

### 開発・レビュー固有（既存 5 件）

現行 `CLAUDE.md` のうち、コード・PR に閉じていると判断したもの。

- コメント・docstring・識別子の作法（`## 出力スタイル`）
- テスト期待値ファイルの一括再生成で予期しない差分が出たら止めて調査する（`## 協働スタイル`）
- 実装は Top-Down（呼び出し側→定義）で提示し、未定義関数は契約を説明してから書く（`## 協働スタイル`）
- テスト結果は passed/failed 数の summary を明示する（`## 協働スタイル`）
- formatter・linter をコミット前に変更ファイルへ適用する（`## 環境・ツール`）

これに加えて、新設した `## 開発・レビュー時` 節の可読性優先条項。

### 領域非依存（大半）

伝達・協働の規律。分割すると逆に穴が空くので、グローバルに残す。

過不足のなさ / 推測・未実測の明示 / 肯定と懸念を同じ明確さで述べる / 質問すべてに明答し
回答をターン最終メッセージに置く / ツール呼び出し直上の意図宣言 / 無評価の差し替えをしない /
一人称 "I" と実在しない "we" の禁止 / 内部呼称・独自語の扱い / 鸚鵡返し回避 / 他者向け文面は
日本語で起こして校正 / 折り返し改行を入れない / 日本語・英語のみ / メモリのスコープ判断と
MEMORY.md の仕掛中ポインタ / 組織固有事実は reference を読む / zsh のグロブ / 長時間コマンドの扱い

## 検討した選択肢

| | 内容 | 評価 |
|---|---|---|
| A | CLAUDE.md 内に領域別の節を作る | 常に読み込まれるので文脈量は減らないが、**適用範囲の印が付く**ので失敗 1 が消える。変更は最小。`## 開発・レビュー時` として着手済み |
| B | 開発固有条項をスキルへ移す | スキルは**呼び出されたときしか読み込まれない**。実装を扱うスキルも無いため穴が残る。不採用 |
| C | 領域別ファイル + CLAUDE.md に 1 行ポインタ | 既存の「組織固有事実は `~/.claude/reference/` を読む」と同じパターン。呼び出しに依存しない。ただし**読む契機の具体性に信頼性が依存する**。曖昧な契機だと読まれないので、契機は具体的な行為で書く必要がある |
| D | 領域別サブ Agent（ツール権限も分離） | **推奨候補**。Agent 定義本体が無条件に効く領域別指示の置き場になり、C の「契機を思い出す」問題が消える。領域の選択がユーザーの明示指定になるので失敗 1 が構造的に消える。加えてツール制限という他案に無い性質を持つ |

### D を推す最大の理由

指示衝突の話とは独立に、**ツールアクセスの分離**そのものに価値がある。採用活動は個人情報を
扱う一方、同一セッションにリポジトリ書き込み権限や外部送信ツールが同居していると露出面に
なる。ツールを絞った人事系 Agent はこれを構造的に制御する。

### D の限界（過大評価しないための注意）

1. **どの条項が領域固有かを分類する作業は消えない。** Agent は容器であって、分類の代替ではない
2. **普遍部分の重複が新しいリスクになる。** 各 Agent 本体に書くと乖離する（→ だから上記の
   未確認事項の答えが重要）
3. **委任しない直接依頼の経路は残る。** グローバルの既定を正しくする必要は消えない
4. **対話的な作業には向かない。** サブ Agent は完了して報告を返す形で、ツール出力も呼び出し側の
   視界に入らない。「境界のあるタスクを片付ける」用途に合い、「一緒に考える」用途（レビュー等）
   には合わない。レビューのような往復のある作業は主セッションに残す
5. **スキルを個別に許可／不許可できるかは未確認。** ツール単位の制限（`Skill` ツール自体を
   持たせるか否か）が可能なことは確かだが、スキルごとの allowlist の有無は確認していない

## 次の一手

1. 未確認事項（グローバル CLAUDE.md のサブ Agent 継承）を確認する
2. 引き継がれる場合: `~/dotfiles/.claude/agents/` を作り、領域別 Agent（開発・レビュー /
   採用 / データ解析）を定義して `~/.claude/agents` から symlink。開発固有条項を開発 Agent へ移す
3. 引き継がれない場合: 選択肢 A に留め、開発固有 5 件を `## 開発・レビュー時` 節へ移動する。
   既存文言の再配置なので、意図せぬ意味変化がないか 1 件ずつ確認する
4. 採用領域の条項は、実際に溜まってから起こす

## 未決の判断事項

- 領域の粒度（「開発・レビュー」を 1 つにするか、実装とレビューを分けるか）
- 既存 5 件の移動を一度に行うか、段階的に行うか
- 主セッション（Agent に委任しない場合）の既定をどの領域とみなすか
