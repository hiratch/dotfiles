PR の Description を作成してください。レビュアーが `review.md` の Step 1 で問題ないと判定できる**最小限**の内容を目指します。極力シンプルに、ソースコード/コメント側に残すべき詳細は重複させないでください。

## テンプレート確認（最優先）
リポジトリに `pull_request_template.md`（`.github/pull_request_template.md`、ルート、または `docs/` 配下など）が存在する場合、**そのテンプレート構造を必ず踏襲**してください。テンプレートのセクション見出しを維持したうえで、以下のガイドラインに沿って中身を埋めます。テンプレートが無い場合のみ、以下のデフォルト構造（Purpose / Means / Verification / Out of scope）を使ってください。

## 必ず含める3要素
1. **Purpose（目的・なぜ）**: なぜこの変更が必要か。1〜3文。
2. **Means（手段・何をしたか）**: 設計判断の核心のみ。実装の核となるアイデアと、複数選択肢から選ぶ際の判断基準があれば短く。
3. **Verification（検証）**: 動作確認の方法・テスト計画。

## 原則

### 書くべきでないこと
- diff を見れば分かる事実（変更ファイル一覧、関数名の列挙、行数等）
- docstring/コメントで表現できている API 詳細
- 「なぜ」が自明な実装選択
- 命名・構造の意図（identifier 自体で表現できている場合）

### 書くべきこと
- **非自明な設計判断の理由**（他の選択肢を退けた根拠）
- **out of scope の境界**（同じ問題に取り組む別PR・follow-up Issue）
- 検証時に**レビュアーが気にすべき観察事実**（例: 「異なる結果が観察された」など、PR本文に残しておく価値のある気付き）

### コード/コメント側に委ねるべきもの
- API の細かい仕様 → docstring
- 既知の制約・TODO → コード内 comment + follow-up Issue
- 設計の trade-off の詳細議論 → 必要なら commit message 本文 / ADR

## 言語の扱い
- **校正・ブラッシュアップは日本語**で行ってください（ユーザーとの対話）
- **最終的な PR 本文の言語**は、リポジトリの慣習（既存PR・テンプレート言語）と PR の対象読者を考慮し、必要に応じてユーザーに確認してください
- 不確かなときは「英語にしますか、日本語にしますか」と短く尋ねる

## ステップ
1. `git log <base>..HEAD --oneline` と `git diff <base>...HEAD --stat` で commit と差分を確認
2. `pull_request_template.md` の有無を確認し、あれば構造を踏襲
3. 関連 Issue・前提 PR の有無をユーザーに確認
4. **日本語**でドラフトを書く（ユーザーとの校正のため）
5. 以下のチェックリストで自己評価を提示（review.md Step 1 と整合させる）:
   - 目的・手段・検証が明記されているか
   - 論理的に明快・一貫性があるか
   - **不要な詳細でレビュアーの時間を奪っていないか**
   - **コード側に委ねるべき内容を Description に書いていないか**
6. ユーザーフィードバックで仕上げる
7. 最終出力時、**言語を確認**してから（必要に応じて英語に翻訳して）出力する

$ARGUMENTS
