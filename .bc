pi=4*a(1)

define rad(x) {
  return x * pi / 180
}

define deg(x) {
  return x * 180 / pi
}

define t(x) {
  return s(x) / c(x)
}

define sin(x) { return s(x) }
define cos(x) { return c(x) }
define tan(x) { return t(x) }
